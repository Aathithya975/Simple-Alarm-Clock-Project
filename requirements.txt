# Core (built-in — no install needed)
# tkinter   — GUI (comes with Python)
# threading — multi-threading
# datetime  — time handling
# winsound  — Windows beep (built-in on Windows)

# Optional: better audio support
pygame==2.5.2
